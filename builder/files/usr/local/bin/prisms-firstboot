#!/bin/bash
set -ex

for file in /etc/firstboot.d/*; do
  source "$file"
done

# do not run firstboot scripts after first time again
touch /etc/prisms-firstboot_not_to_be_run

echo 'Rebooting in 30 seconds' >> /dev/kmsg
sleep 30
reboot
